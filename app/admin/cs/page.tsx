import { redirect } from "next/navigation"; import { AdminLayout } from "@/components/admin/AdminLayout"; import { AdminCsManager } from "@/components/admin/AdminCsManager"; import { getAdminSession } from "@/lib/auth/admin";
export const dynamic = "force-dynamic";
export default async function AdminCsPage() { const session = await getAdminSession(); if (!session.ok) { if (session.reason === "not-logged-in") redirect("/login?next=/admin/cs"); redirect("/forbidden"); } return <AdminLayout admin={{ name:session.profile.name,email:session.user.email,role:session.profile.role }} active="cs" title="CS 처리센터" subtitle="접수 · Workflow · SLA · 고객 타임라인"><AdminCsManager /></AdminLayout>; }
