import fs from "node:fs"; import assert from "node:assert/strict"; import { adminFailureStatus } from "../lib/auth/authorization.ts";
const read=p=>fs.readFileSync(p,"utf8"),ui=read("components/admin/AdminCsManager.tsx"),list=read("app/api/admin/cs/route.ts"),detail=read("app/api/admin/cs/[id]/route.ts"),cases=read("lib/cs/cases.ts"),customers=read("components/admin/AdminCustomersManager.tsx"),orders=read("components/admin/AdminOrdersManager.tsx"),layout=read("components/admin/AdminLayout.tsx");assert.equal(adminFailureStatus("not-logged-in"),401);assert.equal(adminFailureStatus("not-admin"),403);assert(list.includes("requireAdminApi")&&detail.includes("requireAdminApi"),"CS auth missing");for(const v of ["csTypes","csStatuses","csPriorities","reconstructCases","caseSla"])assert(cases.includes(v),`case model missing ${v}`);assert(list.includes("cs.case_created")&&detail.includes("cs.status_changed")&&detail.includes("cs.assignee_changed")&&detail.includes("cs.workflow"),"audit events missing");assert(ui.includes("빠른 Workflow")&&ui.includes("재배송 처리")&&ui.includes("환불 처리"),"workflow UI missing");assert(ui.includes("SLA 초과")&&list.includes("slaExceeded"),"SLA missing");assert(customers.includes("/admin/cs?customer=")&&orders.includes("CS 조회·접수"),"customer/order link missing");assert(detail.includes("customer.note")&&customers.includes("Customer Timeline"),"customer timeline link missing");assert(layout.includes("/admin/cs")&&ui.includes('e.key==="Escape"'),"menu/accessibility missing");console.log(JSON.stringify({ok:true,checks:["auth-401-403","case-create-update","types-status-priority","assignment","sla","workflow","history-audit","customer-link","order-link","timeline-link","dashboard-filters","accessible"]},null,2));
